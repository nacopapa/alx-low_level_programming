#include <stdio.h>
/**
 * add - a function that adds two integers and returns the results
 * @x: int integer type
 * @y: int integer type
 * Return: Return results of 2 integers
 */

int add(int x, int y)
{
	int z = x + y;

	return (z);
}
